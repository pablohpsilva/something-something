# Database
DATABASE_URL="postgresql://username:password@localhost:5432/database"

# Security tokens
CRON_SECRET="dev-cron-secret"
INGEST_APP_TOKEN="dev-app-token"

# Stripe
STRIPE_SECRET_KEY="sk_test_xxx"
STRIPE_WEBHOOK_SECRET="whsec_xxx"

# Server config
PORT="8787"
LOG_LEVEL="info"
NODE_ENV="development"

# Rate limiting config (optional - defaults provided)
RATE_LIMIT_WINDOW_MS="60000"
RATE_LIMIT_EVENTS_PER_IP="60"
RATE_LIMIT_CRAWL_PER_TOKEN="30"

# Deduplication config (optional - defaults provided)
VIEW_DEDUPE_WINDOW_MS="600000"

# Rollup config (optional - defaults provided)
ROLLUP_DAYS_BACK="7"
TRENDING_DECAY_LAMBDA="0.25"